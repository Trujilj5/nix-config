return {
  "RRethy/base16-nvim",
  lazy = false,
  priority = 1000,
  config = function()
    -- This plugin will be configured by Stylix
    -- No manual setup needed as Stylix handles the colorscheme
  end,
}